/**
 * Illustration strings for the MiTow redesign, exported as SVG and rendered through
 * `SvgXml` (`react-native-svg`), the way `design/icons/MiLineIcon.tsx` renders the icon set.
 * Same reason as there: a bundled `require()`d PNG cannot be recoloured or scaled losslessly,
 * and the design draws these as vectors.
 */
export { noBookingsIllustration } from './noBookings';
export { noNotificationsIllustration } from './noNotifications';
export { avatarDefaultIllustration } from './avatarDefault';
