/**
 * Bump this whenever the privacy policy or terms of service copy changes
 * materially. ⚠ A bump ASKS EVERY CUSTOMER AGAIN: consent is checked per
 * account against this version (`GET /me/consent`, `hasAgreedTo`), so an
 * agreement to an older version no longer counts.
 */
export const POLICY_VERSION = '2026-08-10';
