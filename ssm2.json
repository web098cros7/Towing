{
  "commands": [
    "sudo -u ec2-user -i bash -c \"cd /home/ec2-user && if [ ! -d Towing ]; then git clone https://github.com/web098cros7/Towing.git; fi && cd Towing && git config --global --add safe.directory /home/ec2-user/Towing && git fetch origin main && git reset --hard origin/main && sudo bash deploy.sh fresh\""
  ]
}
